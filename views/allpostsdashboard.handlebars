{{!-- dashboard
{{#each posts as |post|}}
    <div class="card entry-card col-10">
        <div class="card-body">
             <h4 class="card-title entry-card-title">{{post.title}}</h4>
             <p class="card-text">{{post.content}}</p>
             <h5 class="card-subtitle mb-2 text-muted">Posted by {{post.user.username}}</h5>        
        </div>
    </div>
{{/each}}
 --}}

